name: w5_flutter_app
description: A Flutter application for monitoring and controlling IoT devices.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  provider: ^5.0.0
  # chart library removed (invalid): add one later if needed, e.g. charts_flutter or fl_chart

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true